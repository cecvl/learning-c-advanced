#include <stdio.h>
#include <stdbool.h>

main(){
 
    //logical operators
    //boolean
    bool x = true;
    bool y = false; // because this is false x&&y will be zero
    printf("xx && yy = %d", x&&y);

    return 0;

}
//need to read more